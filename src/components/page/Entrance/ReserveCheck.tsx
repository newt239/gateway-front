import * as React from 'react';
import { Container, Box, Typography } from '@mui/material';

export default function ReserveCheck() {
    return (
        <Container sx={{ width: '100%' }}>
            <Typography variant='h3'>予約情報照会</Typography>
            <Box sx={{ mt: 2 }}>
            </Box>
        </Container>
    );
}