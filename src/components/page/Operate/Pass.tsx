import * as React from 'react';
import Box from '@mui/material/Box';

export default function Pass() {
    return (
        <Box sx={{ width: '100%' }}>
            <div>pass</div>
        </Box>
    );
}