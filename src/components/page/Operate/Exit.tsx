import * as React from 'react';
import Box from '@mui/material/Box';

export default function Exit() {
    return (
        <Box sx={{ width: '100%' }}>
            <p>exit</p>
        </Box>
    );
}